# Production configuration overrides
debug: false
log_level: "WARNING"
log_json_format: true

data_dir: "data"
model_dir: "models"

model_type: "random_forest"
test_size: 0.2
random_state: 42
n_estimators: 200
max_depth: 15

window_sizes: "5,10,30"

api_host: "0.0.0.0"
api_port: 8000
